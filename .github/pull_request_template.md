## What changed

## Verification
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes for the affected workspace
- [ ] `cargo build && cargo test` passes (if contracts changed)
- [ ] No leftover conflict markers or commented-out debris
- [ ] Call sites updated if a function signature changed